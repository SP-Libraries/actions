name: Prepare

on:
  workflow_dispatch:
  pull_request:
    branches:
      - main
      - develop

jobs:
  prepare:
    uses: ./.github/workflows/code-quality.yml
    with:
      VERBOSE: true
      LINT_PHP: false
      LINT_NODE: true
    secrets:
      GH_TOKEN: ${{ secrets.GH_TOKEN }}
